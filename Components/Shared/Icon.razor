<i class="@(ReturnPreparedHtmlClass())" style="font-size: @(Size ?? 23)pt"></i>

@code {

    [Parameter] public required string Name { get; set; }

    [Parameter] public int? Size { get; set; }

    private string ReturnPreparedHtmlClass()
    {
        return $"fas {Name}";
    }

}